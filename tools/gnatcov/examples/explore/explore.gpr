with "common";

project Explore extends "base" is
   for Source_Dirs use ("src");
   for Object_Dir use "obj/" & Common.Target;

   for Main use ("explore.adb");

   package IDE renames common.IDE;
end Explore;

