with Support, Points, Silent_Last_Chance; use Support, Points;

procedure Test_Points_1 is
   P1 : Point;
begin
   Set (P1, X => 0, Y => 1);
   Set (P1, X => 0, Y => 1);
end;

--# points.adb
--  /bodySet/  l+ 0
--  /preSet/   l+ 0

