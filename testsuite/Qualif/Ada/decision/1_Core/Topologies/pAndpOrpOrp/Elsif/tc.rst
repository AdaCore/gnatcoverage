**"(A and then B) or else (C or else D)", used to control the ELSIF part of an IF statement**

Check DC for ``(A and then B) or else (C or else D)`` used to control the ELSIF part of an IF statement.
