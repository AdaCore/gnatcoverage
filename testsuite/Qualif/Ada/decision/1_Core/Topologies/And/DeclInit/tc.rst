Check DC on ``A and then B`` used as a local boolean object initializer.
========================================================================

Check DC on ``A and then B`` used as a local boolean object initializer.
