Check DC on ``A and then B`` used as part of a record component aggregate
=========================================================================

Check DC on ``A and then B`` used as part of a record component aggregate
initializer.
