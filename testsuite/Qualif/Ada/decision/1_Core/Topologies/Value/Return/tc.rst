Check DC on a standalone Boolean variable ``B`` used as a straight return
=========================================================================

Check DC on a standalone Boolean variable ``B`` used as a straight return
statement value.


