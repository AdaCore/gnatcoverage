"(A or else B) or else (C and then D)", used to control an EXIT-WHEN statement
===============================================================================

Check DC for ``(A or else B) or else (C and then D)`` used to control an EXIT-WHEN statement.
