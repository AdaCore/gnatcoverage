This test case checks that a declaration of an object of a type derived from a
record type with initialization expressions provided for components is
reported as covered only if the declaration is elaborated. The object
declaration itself does not have an explicit initialization.
