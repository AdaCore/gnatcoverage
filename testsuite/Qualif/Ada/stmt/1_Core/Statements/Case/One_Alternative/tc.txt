Check that if a CASE statement is executed only once, then the statement
itself and all the component statements of the alternative being executed are
reported as covered, and the component statements of all the other
alternatives are reported as uncovered. The test driver executes the
functional code in such a way that all the CASE statements are executed once.
