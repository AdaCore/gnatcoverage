Check SC assessments over Discrete Subtype Definitions.

Check that a declaration that contains a discrete subtype definition is
reported as uncovered iif the declaration is not elaborated.

Tests contain cases where the subtype definition is the only statement that
can be covered.
