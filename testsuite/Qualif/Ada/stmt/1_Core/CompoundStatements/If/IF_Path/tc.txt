Check that if an IF statement is executed and its IF branch is chosen, then:
* the IF branch and at least the first statement it contains are covered;
* all other branches and the statements they contain are reported as uncovered.
