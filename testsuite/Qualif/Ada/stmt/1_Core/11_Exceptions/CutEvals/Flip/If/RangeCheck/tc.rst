Check SC on ``if not A`` where A involves a range check that
============================================================

Check SC on ``if not A`` where A involves a range check that
may fail at run-time.
