"return not A", where A involves a range check that may fail at run-time
========================================================================

Check SC on ``return not A`` where A involves a range check that
may fail at run-time.
