**"return A and then B", where both conditions involve a range check that may fail at run-time**

Check SC for "return A and then B" where both conditions involve a range check
that may fail at run-time.
