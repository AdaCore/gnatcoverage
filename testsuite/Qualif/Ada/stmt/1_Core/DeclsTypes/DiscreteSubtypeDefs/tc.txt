Check that a declaration that contains a discrete subtype definition is
reported as uncovered if the declaration is not elaborated, and it is not
reported as uncovered if the declaration is elaborated. Tests contain cases
where the subtype definition is the only statement that can be covered.