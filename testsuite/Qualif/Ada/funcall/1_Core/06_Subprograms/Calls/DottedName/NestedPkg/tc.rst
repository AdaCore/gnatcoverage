**Check FCC for calls to subprograms declared in nested packages**

Check the coverage of calls to subprograms in packages and nested packages,
which requires the calls to be made using dotted names.
