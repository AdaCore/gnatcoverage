**Check FCC is not reported for static calls with enum values**

Make use of enum values and check that no call coverage information is
reported, as function and call coverage should not analyse static calls.
